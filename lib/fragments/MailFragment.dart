import 'package:flutter/cupertino.dart';

class MailFragment extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("MailFragment"),
      ),
    );
  }
}